<%= render 'shared/project_masthead' %>
<div class="album text-muted">
  <div class="container">
    <div class="row <%= 'sortable' if logged_in?(:admin) %>">
      <%= render partial: 'project', collection: @projects %>
    </div>
  </div>
</div>